import { ToolNameDirective } from './tool-name.directive';

describe('ToolNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolNameDirective();
    expect(directive).toBeTruthy();
  });
});
